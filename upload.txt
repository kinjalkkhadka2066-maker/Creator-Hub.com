<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>EditHub Resources</title>

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:Arial, sans-serif;
}

body{
    background:#f5f7fa;
    color:#222;
}

header{
    background:#fff;
    padding:18px 8%;
    display:flex;
    justify-content:space-between;
    align-items:center;
    box-shadow:0 2px 10px rgba(0,0,0,.05);
    position:sticky;
    top:0;
    z-index:100;
}

.logo{
    font-size:1.5rem;
    font-weight:bold;
}

nav a{
    text-decoration:none;
    color:#333;
    margin-left:20px;
}

.hero{
    text-align:center;
    padding:70px 20px;
}

.hero h1{
    font-size:3rem;
    margin-bottom:15px;
}

.hero p{
    max-width:700px;
    margin:auto;
    color:#666;
}

.upload-section{
    width:90%;
    max-width:1000px;
    margin:30px auto;
    background:white;
    padding:25px;
    border-radius:15px;
    box-shadow:0 5px 20px rgba(0,0,0,.08);
}

.upload-box{
    border:2px dashed #bbb;
    border-radius:10px;
    padding:40px;
    text-align:center;
}

.upload-box input{
    margin-top:15px;
}

.controls{
    width:90%;
    max-width:1000px;
    margin:20px auto;
    display:flex;
    gap:10px;
    flex-wrap:wrap;
}

.controls input,
.controls select{
    padding:12px;
    border:1px solid #ddd;
    border-radius:8px;
}

.controls input{
    flex:1;
}

.gallery{
    width:90%;
    max-width:1200px;
    margin:20px auto 50px;
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
    gap:20px;
}

.card{
    background:white;
    border-radius:15px;
    overflow:hidden;
    box-shadow:0 5px 15px rgba(0,0,0,.08);
}

.card img,
.card video{
    width:100%;
    height:220px;
    object-fit:cover;
}

.card audio{
    width:100%;
}

.card-content{
    padding:15px;
}

.card-content h3{
    font-size:1rem;
    word-break:break-word;
}

footer{
    text-align:center;
    padding:25px;
    background:white;
    color:#666;
}

@media(max-width:768px){

.hero h1{
    font-size:2rem;
}

header{
    flex-direction:column;
    gap:10px;
}

nav a{
    margin:0 10px;
}
}
</style>
</head>
<body>

<header>
    <div class="logo">EditHub</div>

    <nav>
        <a href="#">Home</a>
        <a href="#">Resources</a>
        <a href="#">Upload</a>
    </nav>
</header>

<section class="hero">
    <h1>Creative Editing Resources</h1>
    <p>
        Upload and manage photos, videos, sound effects,
        transitions, overlays and editing assets in one place.
    </p>
</section>

<section class="upload-section">
    <div class="upload-box">
        <h2>Upload Resources</h2>
        <p>Photos, Videos & Audio Files</p>
        <input type="file" id="fileInput" multiple>
    </div>
</section>

<div class="controls">
    <input type="text" id="searchInput" placeholder="Search resources...">

    <select id="filterType">
        <option value="all">All Resources</option>
        <option value="image">Photos</option>
        <option value="video">Videos</option>
        <option value="audio">Audio</option>
    </select>
</div>

<section class="gallery" id="gallery">
</section>

<footer>
    © 2026 EditHub | Editing Resources Platform
</footer>

<script>
const fileInput = document.getElementById("fileInput");
const gallery = document.getElementById("gallery");
const searchInput = document.getElementById("searchInput");
const filterType = document.getElementById("filterType");

let resources = [];

fileInput.addEventListener("change", function() {

    [...this.files].forEach(file => {

        const reader = new FileReader();

        reader.onload = function(e){

            let type = "";

            if(file.type.startsWith("image")){
                type = "image";
            }
            else if(file.type.startsWith("video")){
                type = "video";
            }
            else if(file.type.startsWith("audio")){
                type = "audio";
            }

            resources.push({
                name:file.name,
                type:type,
                src:e.target.result
            });

            renderResources();
        }

        reader.readAsDataURL(file);
    });
});

function renderResources(){

    const search = searchInput.value.toLowerCase();
    const filter = filterType.value;

    gallery.innerHTML = "";

    const filtered = resources.filter(item => {

        const matchName =
            item.name.toLowerCase().includes(search);

        const matchType =
            filter === "all" ||
            item.type === filter;

        return matchName && matchType;
    });

    filtered.forEach(item => {

        const card = document.createElement("div");
        card.className = "card";

        let media = "";

        if(item.type === "image"){
            media = `<img src="${item.src}">`;
        }

        else if(item.type === "video"){
            media = `
            <video controls>
                <source src="${item.src}">
            </video>`;
        }

        else if(item.type === "audio"){
            media = `
            <audio controls>
                <source src="${item.src}">
            </audio>`;
        }

        card.innerHTML = `
            ${media}
            <div class="card-content">
                <h3>${item.name}</h3>
            </div>
        `;

        gallery.appendChild(card);
    });
}

searchInput.addEventListener("input", renderResources);
filterType.addEventListener("change", renderResources);
</script>

</body>
</html>